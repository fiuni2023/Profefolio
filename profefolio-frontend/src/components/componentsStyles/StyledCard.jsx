import styled from 'styled-components';
import { Card } from 'react-bootstrap';

const SCard = styled(Card)``;
const SHeader = styled.div``;
const SBody = styled.div``;

export {SCard, SHeader, SBody}; 